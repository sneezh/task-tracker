imports:
    - { resource: layer/domain.yaml }
    - { resource: layer/application.yaml }¡
    - { resource: layer/infrastructure.yaml }

services:
    _defaults:
        autowire: true
        autoconfigure: true